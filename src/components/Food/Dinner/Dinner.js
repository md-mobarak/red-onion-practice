import React from 'react';

const Dinner = ({ dinner }) => {
    const { name, img, price, id } = dinner
    // console.log(dinner);
    return (
        <div>

        </div>
    );
};

export default Dinner;